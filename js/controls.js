export const myRange = (id) => document.getElementById(id);
export const changeSize = (size, pelotas) => pelotas[0].side = size;
export const changeSpeedX = (speedX, pelotas) => pelotas[0].vx = speedX;
export const changeSpeedY = (speedY, pelotas) => pelotas[0].vy = speedY;
